# Telegram
BOT_TOKEN=
# Supabase (pooled/pgbouncer, port 6543)
DATABASE_URL=
# Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
# Vercel cron
CRON_SECRET=
WEBHOOK_URL=
WEBHOOK_SECRET=
# Telegram Mini App (register with BotFather /newapp; t.me deep-link base)
MINIAPP_URL=
# Chat where access requests land and the admin approves/bans (your own DM or a
# private admin group). Negative for a group. Find it via @userinfobot or logs.
# NOTE: anyone in this chat can approve/ban — keep it to people you trust.
ADMIN_CHAT_ID=
